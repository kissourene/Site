import React from 'react';

export default function ProductCard({ product }) {
  return (
    <div className="product-card">
      {/* Display product details here (name, price, image, etc.) */}
    </div>
  );
}